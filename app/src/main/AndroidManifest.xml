<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mk_kadish.mk3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".game4_HomeActivity"></activity>
        <activity android:name=".game3_HomeActivity" />
        <activity android:name=".game2_HomeActivity" />
        <activity android:name=".game1_HomeActivity" />
        <activity android:name=".indexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".userManagementActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".startActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>